# UI-001 — Design System & Application Shell

**Status:** delivered — **PR open, awaiting architectural review. Not merged.**
**Base:** `d9db6af` (main, after SEC-001)
**Branch:** `claude/ui-001-design-system`

---

## Scope, and why it stops where it does

This ticket is the hinge the project's own vision identifies: do not let twenty
screens be built before a coherent design system exists, because whatever the
first screen establishes becomes the convention the other nineteen copy —
correct or not.

So UI-001 delivers the foundation and **one** showcase screen, and deliberately
**no business screen**. No Catalog, no Releases, no Distribution, no Studio, no
Royalties. Those wait for this to be reviewed.

## What is here

**Stack.** Vue 3, TypeScript (strict, with `noUncheckedIndexedAccess`),
Inertia 2, Tailwind 4, Vite 7. `vue-tsc` runs in CI as its own step, so a type
error fails the build rather than being noticed later.

**Tokens.** Colour, typography, radius, elevation, z-index and motion, all
semantic — `--color-surface`, never `--color-zinc-900`. Light and dark, plus
follow-the-system. The full reasoning is in `docs/adr/ADR-0013`.

**Primitives.** Button, Card, MetricCard, StatusBadge, Alert, Modal,
ConfirmDialog, Skeleton, ProgressBar, EmptyState, ErrorState, CodeValue,
FormField, TextInput, TextareaInput, SelectInput, ToggleInput, DataTable with
header/cell, CursorPagination, NavIcon, Sidebar, and the AppLayout shell.

**Shell.** Fixed sidebar on desktop, drawer below `lg`, sticky top bar, skip
link, theme switcher, flash messages. Navigation built on the server.

**One screen.** `/design-system` — every primitive in both themes.

## Decisions worth challenging

Ten are set out in `docs/adr/ADR-0013-design-system-foundations.md`. The four
most consequential:

**Semantic tokens rather than a colour scale**, so dark mode is a redefinition
of names rather than a second set of classes through every template.

**Status colour is decided in exactly one component.** Every module has its own
status enum; without one place deciding what "in progress" looks like, the same
idea ends up amber on one screen and blue on another. The *label* always comes
from the translations — showing `WAITING_CAPABILITY` to a user is showing them
the database.

**Navigation is built on the server, and unbuilt sections are shown as
disabled** rather than hidden. A navigation that grows as features land is
disorienting. `available` is presentation only and never the authorisation.

**Null renders as an em dash, never zero.** `MetricCard` takes `number | null`.
"No failed jobs" and "we could not count the failed jobs" are different facts,
and showing `0` for both hides an outage.

## No invented data — enforced, not promised

The showcase queries nothing. Its metric cards pass `null`, so they render em
dashes, and the page carries a visible notice saying the placeholders are
placeholders. A showcase displaying "1,284 tracks" would be exactly the
fabricated figure the platform's rules forbid, and it would be screenshotted.

## Everything is behind SEC-001

`/design-system` sits in a route group carrying `auth` and `active`. Two tests
assert it: a guest is redirected to sign-in, and an account deactivated
mid-session loses access at the *next request* rather than when its remember-me
cookie expires.

`/` now redirects — guests to sign-in, signed-in people to the interface.
SaniTube has no public face.

## Nothing secret reaches the browser

The internal API token, storage keys and provider keys stay on the server. A
test puts a realistic token and an OpenAI-shaped key into config, renders a
page, and asserts neither appears anywhere in the response — then walks every
shared prop key against a list of credential names.

The first version of that key list contained a bare `key`, which matched the
navigation items' own `key` field. Narrowed to unambiguous names: this is the
same false positive ING-001 hit with `path`, and a check that cries wolf is one
people start deleting.

## Accessibility

`:focus-visible` rather than `:focus`. Dialogs move focus in, trap it and
return it. Real `<table>` semantics. `role` chosen by severity so only danger
and warning interrupt a screen reader. `prefers-reduced-motion` honoured. Skip
link. Toggle is a `role="switch"`, not a styled checkbox.

## The dependency constraint, recorded

`@vitejs/plugin-vue@5` declares a peer range of Vite `^5 || ^6`; the project is
on Vite 7, and npm refused.

**Resolved by using `@vitejs/plugin-vue@6`, which supports Vite 7 — not by
`--force` or `--legacy-peer-deps`.** Forcing would have installed a plugin
against a Vite major it was never tested on, and that failure mode is a build
that works until it silently does not. A peer conflict is information about
compatibility; overriding it discards the information and keeps the
incompatibility. Same for `vue-tsc@3`.

This will recur — the frontend toolchain moves faster than the peer ranges
declared against it. Recorded in the ADR.

## Translations

All new strings ship in en, fr, es, it, pt and de. The completeness gate
generalised in SEC-001 now covers `ui.php` automatically, in both directions.

Shared from the server as a **flat map with dotted keys**, so `trans('ui.actions.save')`
is one lookup rather than a walk down an object. A missing key returns the key
itself: blank text looks like a layout bug and gets ignored.

## Architecture amendment

**None.** No migration, no column, no relation, no invariant. One new module
(`Ui`), one route, the frontend toolchain, and a `Type-check` step added to the
existing frontend CI job.

## Deferred, and named

The mission's component list also names Breadcrumbs, Tabs, Dropdown, Popover,
Tooltip, Toast, Radio, Checkbox, SearchInput, CoverImage, TrackIdentity,
ArtistIdentity and AudioPlayerShell. They are **not** in this PR.

Each is shaped by the screen that first needs it — a Toast queue's behaviour
depends on what raises toasts, and `TrackIdentity` depends on what a track row
must show. Building them now against a guess is how a component library
acquires a dozen unused variants. They arrive with UI-002 onwards, on the token
system this ticket establishes.

## Tests

**12 new tests. 599 tests / 2131 assertions**, 1 skipped (the real ffprobe
run). PHPStan level 6 clean. Pint clean. `vue-tsc` clean. `npm ci` reproduces
from the committed lockfile and builds.

| Requirement | Test |
| --- | --- |
| routes behind SEC-001 | `every_interface_route_is_behind_authentication` |
| deactivation ends access | `a_deactivated_account_loses_the_interface_at_the_next_request` |
| showcase renders | `the_showcase_renders_for_a_signed_in_person` |
| **no credential in the browser** | `no_credential_is_shared_with_the_browser` |
| no password hash shared | `the_signed_in_person_is_shared_without_their_password_hash` |
| translations resolved server-side | `translations_are_shared_resolved_for_the_active_locale` |
| navigation is honest | `the_navigation_is_built_on_the_server_and_marks_what_is_not_ready` |
| role hides admin section | `a_member_is_not_offered_the_administration_section` |
| guests get no navigation | `a_guest_receives_no_navigation_at_all` |
| root redirects | `ExampleTest::the_root_sends_a_stranger_to_sign_in` |
| translation completeness | covered automatically by the SEC-001 gate |
| template and Vite config agree | `ViteEntrypointTest::the_template_and_the_vite_config_agree_on_the_entry_points` |
| entry points exist on disk | `ViteEntrypointTest::every_entry_point_is_a_file_that_exists` |

## Review corrections C1–C4

The verdict was CORRECTIONS REQUIRED on accessibility and lifecycle. All four
are implemented.

**C1 — mobile drawer accessibility.** The drawer declared
`role="dialog" aria-modal="true"` without owing behaviour. It now has it, and
**not** through a second focus trap: the behaviour was extracted into
`useModalSurface`, which `AppModal` also uses. Focus moves in, Tab and
Shift+Tab wrap, Escape closes, focus returns to the menu trigger, the
background is locked and marked `inert`. Backdrop click and body scroll
locking are retained.

**C2 — modal scroll and background.** Background scroll locking added, with
cleanup on unmount. The stale-lock case is closed two ways: the lock is
released on unmount as well as on close, and it is **counted** at module level
so two stacked surfaces cannot have the first to close unlock a page the second
still covers.

**C3 — lifecycle cleanup.** `router.on('navigate', …)` returns an unsubscribe
function; it is now kept and called on unmount. Body overflow and the `inert`
attribute are restored on unmount. Nothing global is left behind.

**C4 — tests.** Behavioural, not structural: Vitest with jsdom and
`@vue/test-utils` was added, wired into the frontend CI job as `npm run test`.
Sixteen specs cover every behaviour the correction names.

| Requirement | Test |
| --- | --- |
| drawer receives focus | `moves focus into the drawer when it opens` |
| Escape closes | `closes the drawer on Escape`, `closes on Escape` |
| Tab wraps | `wraps Tab from the last control back to the first` |
| Shift+Tab wraps | `wraps Shift+Tab from the first control back to the last` |
| focus returns to trigger | `returns focus to the menu trigger after the drawer closes` |
| body scroll lock restored | `locks and restores background scroll around the drawer` |
| modal scroll lock restored | `locks background scroll while open and restores it on close` |
| lock survives stacking | `keeps the page locked until the last of two stacked surfaces closes` |
| no stale lock on unmount | `releases the background when unmounted while still open`, `restores background scroll when unmounted with the drawer open` |
| no listener accumulation | `does not accumulate navigation listeners across remounts` |
| background made inert | `marks the application root inert while open` |
| UI works without a Vite build | `Tests\TestCase` + the seven Composer-only CI jobs |
| CI renders against a real manifest | `frontend` job, manifest asserted before the run |

Two limits are stated in the specs rather than papered over: jsdom does not
implement what `inert` *does*, so only the attribute's application and removal
are asserted; and jsdom does not focus a button on click, so the trigger is
focused explicitly where focus restoration is under test.

**The three lifecycle specs were verified to fail against the pre-correction
code** — listener leak and stale scroll lock — before being trusted. No
existing test was weakened or skipped.

## The first CI run was red, and it was right

The first run of this PR failed seven of ten jobs. Every failure was the same
line: `Vite manifest not found at public/build/manifest.json`. The PHP jobs
never run `npm run build`, and `public/build` is a gitignored artefact — so
`@vite` threw on every test that renders a page.

**The local suite had been green for a reason that proved nothing**: a bundle
built earlier was still sitting on the disk. This is the same class of mistake
as the CAT-001 rollback check that passed on SQLite and said nothing about
MySQL. The local run did not exercise the case CI was exercising, and reporting
it as evidence would have been reporting a coincidence.

What was *not* done: the tests were not weakened, skipped, or moved off the
rendering path. What was done is in ADR-0013 — Vite is stubbed when no manifest
exists so the PHP suite runs after a plain `composer install`; the `frontend`
job builds the bundle and renders the interface against the real manifest,
asserting the manifest exists first so the step cannot degrade silently; and
`ViteEntrypointTest` statically covers what a stub cannot, verified to fail on a
deliberately mismatched template before being trusted.

Verified locally in both states before pushing: bundle absent → 599 passed;
bundle present → 599 passed, with the eleven interface tests exercising the real
manifest.

## Not merged

Per the restored review rule: implemented, CI green, PR open, report written,
**stopping here for architectural review.**

## External blockers

`EXTERNAL_ADMIN_ACTION_REQUIRED` — the repository default branch is still
`claude/verifier-repertoire-git-k29ft2` and should be `main`. Unchanged; no
tool in this environment can alter repository settings.

STO-002, AI-002, GEN-002 and DIST-002 remain outstanding and do not block this.
